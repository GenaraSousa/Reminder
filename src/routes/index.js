import { NavigationContainer } from '@react-navigation/native';
import React from 'react';
import StackRoutes from './stackRoutes';

const Routues = () => (
    <NavigationContainer>
        <StackRoutes />
    </NavigationContainer>

)

export default Routues;